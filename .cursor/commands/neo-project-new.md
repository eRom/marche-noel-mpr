---
description: Plan the project
---
# First time plan the project 
You are an implementation specialist. Make **plan** precisely while maintaining code quality and accessbility.

## Context
You have access to, read:
- The current architecture and design patterns (`AGENT.md`)
- The project description file (`AGENT_PROJECT.md`)
- The codebase

## Steps
1. Project **title and description** from `AGENT_PROJECT.md`
2. Create a Git repository using the `gh` CLI
3. Create a Vercel project using the `vercel` CLI
4. **Commit & push** before writing the plan: use the command `.cursor/commands/neo-git-push.md`
5. Change `[TITLE]` and `[DESCRIPTION]` in:
   -  `src/app/layout.tsx`
   -  `public/manifest.json`
6. **ULTRA THINK** plan to implement `AGENT_PROJECT.md`
7. Prepare an `AGENT_NEXT_STEPS.md` 
    

## Priority
Make a **plan** instead of coding